# Story 2.6 — Engine worker host + transport hardening — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** the engine runs in a dedicated Worker behind the same `EngineClient` (contract v2: serializable session protocol with explicit lifecycle + row economy); the transport is production-grade; FileFormat/FileSource excised; luxon/xlsx build discipline proven; `./qa` sunset.

**Architecture:** Order matters: excision FIRST (Task 1 — it slims the service the protocol wraps; doing it later would mean protocolizing dead surface), then protocol+transport (Tasks 2–3, pure client-side, testable with a fake "worker"), then the worker host + composition root (Task 4, the real thread hop via @vitest/web-worker), then the app swap + sunset + build checks (Task 5), then E2E + final gate (Task 6). Boundary spec changes EXACTLY as declared (./qa dies, createWorkerEngineClient joins) — exactness stays the invariant.

**Tech Stack:** no new runtime deps. @vitest/web-worker (already used by the 1.1 spike test) for real-hop tests; Vite worker bundling (`new Worker(new URL('./engine-worker.ts', import.meta.url), {type:'module'})`); existing Workbox precache config.

**Source spec:** `docs/superpowers/specs/2026-06-12-story-2.6-worker-design.md` (4 locked decisions + 2 protocol refinements, all pins)
**Branch:** `story/2.6-worker`. **Flow:** tasks → dev final review (checkpoint (c)) → STOP, hand back to founder (role fence — dev does NOT run QA).

---

## Task 1: Excise FileFormat/FileSource (decision 3, TDD-by-suite-adaptation)

**Files:** DELETE `internal/importStatement/dao.ts` · MODIFY `internal/importStatement/service.ts` (constructor slims: drop fileFormatDAO/fileSourceDAO params; delete `_applyTransformationRules`, `_saveFileFormatAndSource`, `_loadFileFormatsAndSources`, `_calculateInitialMatchPercentages`, `_findBestMatchingFormat`, the stage3-save call site, `lastUsed: Date.now()` with them) · MODIFY `internal/importStatement/types.ts` (FileFormat/FileSource types + transformations types die) · MODIFY stage2/stage3 internals that reference sources (`sourcesWithFullMatch`, `selectedSource` plumbing — trace and excise; stage3's `selectedSource` getter stubs to `null` with an S3a-redefines note) · ADAPT all touched suites + the E2E stub-DAO helpers (pipeline-e2e, real-statements).

- [ ] Trace first: `grep -rn "FileFormat\|FileSource\|fileFormat\|fileSource\|sourcesWithFullMatch\|selectedSource\|transformations" packages/engine/src --include="*.ts"` — enumerate every site in the report BEFORE cutting.
- [ ] Excise with discipline (1): every ported-suite assertion that exercised format-recall is adapted with an explicit `// EXCISED (2.6 decision 3):` callout — no silent deletions; enumerate each in the report (QA will diff the suites).
- [ ] Discipline (2), grep-proven: NEW test `excision.spec.ts` — reads the engine src tree and asserts zero `FileFormat|FileSource` identifiers outside comments/docs (a tripwire, like the boundary spec).
- [ ] Determinism check: grep drops to 2 classified sites (user-settings source-id, id generator).
- [ ] Gate + commit: `refactor(2.6): FileFormat/FileSource excised — format recall superseded by the 2.3 pool (FEAT-005; Date.now residue dies)`.

## Task 2: Contract v2 protocol + DTOs (TDD)

**Files:** REWRITE `client/protocol.ts` (envelope + methods + events + CONTRACT=2 + the bump rule documented) · NEW `client/dto.ts` (all DTOs + serializers) · NEW `client/errors.ts` (wire-error codec) · MODIFY `client/engine-client.ts` (the grown interface) · keep `direct-client.ts` implementing the same grown interface in-thread.

- [ ] `protocol.ts`: `CONTRACT_VERSION = 2`; bump rule in the header (ANY change to method set/signatures/envelope bumps — additions included); envelope: `hello {contract}` / `helloAck {contract}` / request `{id, method, args}` / response `{id, ok, value|error}` / event `{event, payload}` (no id — out-of-band). Methods: `ping, getVersion, decode, importStart, importApplyColumn, importResetColumn, importConfirmRecall, importResolveCollision, importGetRows, importNext, importAbort`.
- [ ] `dto.ts` (failing tests first — round-trip serialize/deserialize per DTO): `Stage2SnapshotDTO` (columns: id/originalName/definition/params/recallState/sample cells with message KEYS+params; recognized {n,m}; lastSaveCollision; unmapped list) · `ColumnRejectionDTO` (from ColumnTransformRejection: errorCount/totalCount/threshold/cellErrors[{rowIndex, columnId, messageKey, params}]) · `RowWindowDTO {offset, total, rows: TransactionRowDTO[]}` (dates as ISO strings — structured-clone passes Dates but ISO keeps the DTO JSON-safe and pinned) · `GenerateResultDTO {rows, rowErrors, skipped}` · `UnmappedColumnsDTO`. Serializers are PURE functions over the internal objects (tested without a worker).
- [ ] `errors.ts` (failing tests first): `serializeError(err) → {name, messageKey?, params?, payload?}` + `rehydrateError(wire)` → typed instances for `ColumnTransformRejection, UnmappedColumnsError, BaseCurrencyNotSetError, ContractMismatchError, EngineWorkerDiedError, SessionAlreadyActiveError`; unknown → `EngineError` preserving the original name (HC-7, never mangled). Round-trip test per type incl. payload fidelity (cellErrors survive).
- [ ] `engine-client.ts`: the grown `EngineClient` interface + `onEvent(cb): () => void` (plain callback, returns unsubscribe; NO RxJS). `direct-client.ts` implements it in-thread (session registry + the same DTO serializers — shared module, DRY) so vitest/QA paths stay worker-free.
- [ ] Gate + commit: `feat(2.6): contract v2 — session protocol, DTOs, wire-error codec (bump rule documented)`.

## Task 3: Production WorkerTransport (TDD, no real worker needed)

**Files:** REWRITE `client/worker-client.ts` → `createWorkerEngineClient(workerFactory: () => Worker, opts?)`.

- [ ] **Failing tests first** (fake Worker object — a controllable {postMessage, onmessage, onerror, terminate} test double; the REAL hop is Task 4's):
  - handshake: first message out is `hello {contract: 2}`; requests queued until `helloAck`; ack with wrong contract → ALL queued+future calls reject `ContractMismatchError`; **PIN: mismatch pre-job** (no request crosses before ack) + **1-RTT pin** (exactly one hello/helloAck exchange).
  - drain: `onerror` / terminate → every pending request rejects `EngineWorkerDiedError {jobsLost}`; no hung promises (await all, assert rejections).
  - per-request timeouts: handshake 5s, RPC 30s defaults (opts-overridable for tests); a request past its window rejects loudly; **progress liveness**: a `progress` event carrying the request's jobId RESETS the window (long parses never time out while emitting).
  - respawn-on-death: workerFactory re-invoked once after death; NEW handshake; sessions NOT resurrected (an `importApplyColumn` for a pre-death sessionId rejects with the worker's session-unknown error path — asserted in Task 4 over the real hop); `dead` event delivered to onEvent.
  - events: `progress/blocked/dead` reach `onEvent` subscribers; unsubscribe works.
- [ ] Implement: pending map `{resolve, reject, timer, jobId?}`; hello gate queue; drain helper; event fan-out. Deterministic: timers via injectable clock (opts) — no Date.now in assertions path (test seam).
- [ ] Gate + commit: `feat(2.6): production WorkerTransport — handshake gate, drain, timeouts with progress-liveness, respawn`.

## Task 4: Worker host + composition root (the real hop, TDD via @vitest/web-worker)

**Files:** NEW `internal/worker/engine-worker-host.ts` (message loop + session registry) · NEW `internal/worker/sessions.ts` (session lifecycle) · NEW `src/engine-worker.ts` (the Vite worker ENTRY: imports host, self.onmessage wiring) · MODIFY `index.ts` barrel (export `createWorkerEngineClient`; the boundary spec updates in Task 5 with the sunset) · MODIFY `internal/persistence/engine-db.ts` (onblocked → host event hook — additive listener param or module event, report the shape).

- [ ] Host (failing real-hop tests first, the 1.1 @vitest/web-worker pattern): handshake ack with CONTRACT_VERSION; method dispatch over the session registry; **composition root**: real `UserSettingsIDBDAO` + recall pool + rates holder + `ImportStatementServiceImpl(settingsDao, recallPool)` — THE 2.3 MUST-DO dies (delete the unwired-path comments in service.ts).
- [ ] Session lifecycle (founder refinement 1, all pinned): registry holds ≤1 active session; `importStart` while active → `SessionAlreadyActiveError` (PIN); `importAbort`/completed `importNext` frees the graph (assert registry empty after both); sessions die with the worker — post-respawn `importApplyColumn(oldSessionId)` → structured session-unknown error (PIN, over the real hop with a terminated+respawned worker).
- [ ] **Row economy (founder refinement 2, PIN):** `importApplyColumn` returns the column-state snapshot only; `importGetRows` windows. Pin test: serialized snapshot byte-size for a 10k-row synthetic session within 2× of a 12-row session's (column counts equal) — applyColumn cost does not scale with row count on the wire.
- [ ] Progress events: decode + importNext emit `progress {jobId, phase, done, total}` with HONEST counts (HC-10) — assert monotone done ≤ total, final done === total.
- [ ] **Mid-job kill atomicity (decision 1 PIN):** start an import that writes recall-pool entries → terminate the worker mid-flight → reopen the DB → pool contains either none or complete entries, never partial (iterate: each stored entry validates against the entry shape).
- [ ] engine-config over the hop: store override set via setEngineParam BEFORE importStart → the 2.4 gate bites over the worker hop (one test — hydration alive in the worker composition root).
- [ ] Gate + commit: `feat(2.6): worker host — session registry, composition root wired (2.3 MUST-DO dies), honest progress`.

## Task 5: App swap + ./qa sunset + build checks

**Files:** MODIFY `apps/web/src/engine.ts` (worker spawn + handshake + createWorkerEngineClient — THE wiring point) · NEW `apps/web/src/app/chrome/EngineStatusBanner.tsx` (altus-styled, 3 loud states: blocked / mismatch+SW-update / died; minimal, presentational) · MODIFY the SW update path (mismatch state triggers registerSW update check — NOT bare location.reload; reuse the 1.4/1.5 chrome i18n slots for the copy keys) · DELETE `packages/engine/src/qa.ts` + exports-map `"./qa"` · MODIFY `apps/web/src/qa/Harness.tsx` (rides the worker-backed client) · MODIFY `packages/engine/src/boundary.spec.ts` + `apps/web/src/__boundary__/boundary.spec.ts` (EXACT new surface: runtime keys `['createDirectEngineClient','createWorkerEngineClient']`; `./qa` import FAILS) · MODIFY eslint fence (drop the engine/qa exception) + teeth-check.

- [ ] Boundary spec FIRST (failing): exact new runtime keys; `./qa` resolution fails. Then the sunset (delete qa.ts + map entry), then the harness switch.
- [ ] Fence: remove the `engine/qa` exception from the apps/web fence; TEETH-CHECK evidence: a scratch file importing `@abc-budget/engine/qa` AND a deep path both fail lint (capture output in the report, then delete the scratch).
- [ ] `engine.ts` swap: worker spawned ONCE at module init (decision 4 pin), respawn handled by the transport; `EngineStatusBanner` subscribes via onEvent; uk/en keys for the three states (chrome catalog — the 1.4 pattern).
- [ ] **Luxon/xlsx build check (double-tagged since 2.2):** `pnpm build` → assert via the Vite manifest: luxon and xlsx in their own lazy chunks, NOT in the entry or worker-entry initial graph; Workbox precache manifest INCLUDES those chunks; then the offline proof — preview + SW active → airplane-mode (devtools offline) → decode+parse a fixture through the UI harness → succeeds (scripted with the chrome-devtools/playwright pattern used for the 1.1 offline proof; capture evidence).
- [ ] Gate + commit: `feat(2.6): app rides the worker — ./qa sunset (declared boundary change), loud status states, luxon/xlsx offline-proven`.

## Task 6: E2E closure + final gate (→ dev final review → STOP)

- [ ] Real-statements local spec: one variant runs the full flow over the WORKER-backed client (real hop) — counts identical to the direct path (the established hard counts; zero drift).
- [ ] Large-file honesty: 10k-row synthetic fixture (generate.mjs extension) through the worker — progress events flow, monotone, final-equals-total; UI thread free (no sync block > threshold in the test harness — assert via event-loop probe).
- [ ] Clean install (full rm list) → typecheck 0 · lint 0 · engine (857+new) · web 66+new · functions 73 · build green · cold-cache ×2 (the 2.5 acceptance pattern stays green with the worker entry).
- [ ] DoD: boundary spec asserts the EXACT new surface (exactness is the invariant) · `./qa` gone everywhere (grep) · excision grep-proof green · determinism: 2 classified sites · migrations untouched · contract=2 + bump rule documented · web diff ≈ wiring + banner + harness (git diff --stat captured for the report).
- [ ] Dev final review report (per-decision pins + checkpoint-(c) evidence: worker swap, zero UI diff, kill-mid-job, offline parse) → **STOP. Hand back to founder. Dev does NOT dispatch QA (role fence).**

## Carry-forward

S3a lean source notion (FEAT-001/FEAT-009 — from the design bundle's needs) → 2.7 · full UI state matrices → 2.7/2.8 · Q-011 dup-counter → EP-3 (standing tag) · worker perf budget on 10k+ → 9.1.
