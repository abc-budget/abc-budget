# Story 2.3 — Parse rules + recall pool — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** columnName-granularity recall over a persisted pool (migration v3 + settings store), the stage orchestration ported INTACT, the row-generator rebuilt (collect-don't-throw + COUNTERPARTY), the engine pipeline closed end-to-end (decode→stage1→stage2(recall)→typed rows) — proven on fixtures and the real statements locally.

**Architecture:** Port order follows dependencies: settings module + migration v3 + clear-on-reject first (the persistence substrate), then the recall pool (new code on the new store), then the stage orchestration (service/stage1/stage2-impl INTACT with their suites), then the row-generator rebuild, then the E2E closure. Port discipline = 1.2/1.6/2.2 rules (verbatim + declared adaptation classes, hunk-audited, specs along).

**Tech Stack:** no new deps. RxJS (already a transitive prior-art need? — CHECK: prior-art stages import rxjs; the engine package must ADD rxjs as a dependency in Task 3 — internal only, never on the public surface).

**Source spec:** `docs/superpowers/specs/2026-06-11-story-2.3-recall-pool-design.md`
**Prior art (READ-ONLY):** `D:\abc-budget\webapp\libs\engine\src\{importStatement,settings}\`
**Branch:** `story/2.3-recall-pool`. **Flow:** tasks → dev review → QA protocol → PM.

---

## Task 1: Persistence substrate — settings port, migration v3, clear-on-reject (TDD)

**Files:** PORT `internal/settings/{user-settings.ts, user-settings-idb.ts, user-settings.init.ts, types.ts}` + 3 spec files (Container→DbProvider-style adaptations only; diff-audit). NEW `internal/settings/base-currency.ts` (+spec). MODIFY `internal/persistence/engine-db.ts` (+migration v3, clear-on-reject) + `internal/exchange-rate/rates-holder.ts` (clear-on-reject) + `persistence.spec.ts` (anchor 2→3 — THE one legit update, call out in commit).

- [ ] Read prior-art settings module fully; port with the established adaptation classes (ioc→injection; import paths; verbatimModuleSyntax). The `USER_SETTINGS_STORE_CONFIG` (keyPath 'key' + its index config) carries into migration v3 exactly.
- [ ] **Migration v3** — ONE step: `ctx.createStore('userSettings', <prior-art config>)` + `ctx.createStore('recallPool', { keyPath: 'columnName' })`. v1/v2 byte-unchanged. persistence.spec: length 3, v3 creates exactly both stores; fresh≡upgrade re-verified (the 1.2 invariant test pattern at v3).
- [ ] **clear-on-reject (the tagged MUST-DO, TDD):** failing tests first — (a) `openEngineDb`: first open rejects (mock indexedDB.open failure once) → second call RETRIES (new promise) and succeeds; (b) same for the rates-holder's memoized service promise. Implement: on rejection, clear the memoized promise (both layers). Remove/update the 1.6 JSDoc guardrails to reflect DONE.
- [ ] `base-currency.ts`: `getBaseCurrency(): Promise<string>` (reads settings; **unset → throws a loud, specific error** — the 2.7 gate sets it) + `setBaseCurrency(iso)` validating via the 1.6 reference. Spec: set/get round-trip, unset→loud, invalid ISO rejected.
- [ ] Wire `use_base`: locate the 2.2 TODO-2.3/2.4 comment in column.ts — resolve `'use_base'` through `getBaseCurrency()` at the resolution site (REPORT the exact wiring; the call path is already async). Extend the 2.2 ENT-011 test: use_base with a set base resolves it; unset → loud.
- [ ] Gate + commit: `feat(2.3): settings store port + migration v3 + clear-on-reject (both layers) + base currency (use_base live)`.

## Task 2: The recall pool (NEW, TDD — the FEAT-005 rebuild)

**Files:** NEW `internal/importStatement/recall/{pool-dao.ts, recall.ts, recall.spec.ts}`.

- [ ] **Failing suite first** — the spec §2 contract as tests:
  - `normalizeKey`: NFC+trim; **normalize-before-save pin (founder refinement):** save with `'Дата́...'`-style NFD + padded `'  Сума  '` names → `getAllKeys()` shows exactly ONE entry per logical name, each keyed NORMALIZED; lookups by NFD/padded/exact variants ALL hit.
  - Save semantics: new name → saved; identical mapping → no-op ('saved'); same type + different params → `{ collision: { kind: 'params-change', existing, incoming } }`; different type → `kind: 'type-change'`; `confirmSave` → LWW overwrite.
  - `recallFor(names)`: known → prefill entries marked `state: 'guessed'`; unknown → absent (stay UNKNOWN); `recognized: { n, m }` derived; empty pool → n=0.
  - Persistence: close + reopen the test DB → pool intact.
  - `col_N` names key like any other (one test).
  - Auto-detect OFF by default (flag in engine config — extend the 2.2 `engine-config.ts` with `recallAutoDetectEnabled: false`); when ON: deterministic date try-parse (reuse `detectDateFormat`) + status-pattern heuristic produce GUESSED prefills for unknown names; when OFF: untouched. Two tests.
- [ ] Implement `pool-dao.ts` (the 1.2 `IDBBatchDao` subclass pattern — mirror `exchange-rate/dao.ts`) + `recall.ts`. Determinism: no Date.now (no timestamps in entries — LWW is structural).
- [ ] Gate + commit: `feat(2.3): recall pool — columnName→type, NFC-keyed, collision kinds, N-of-M (FEAT-005/013)`.

## Task 3: Stage orchestration — PORT INTACT

**Files:** PORT `internal/importStatement/{service.ts, stage1.ts}` + `stage2/implementation.ts` + ALL their specs (incl. the 1,347-line `implementation.spec.ts`; `service.stage3.spec.ts` ports in Task 4 with the row-generator). MODIFY `packages/engine/package.json` (+rxjs).

- [ ] `pnpm --filter @abc-budget/engine add rxjs` — INTERNAL only (comment at first import site: 1.1 rule, no Observables on the public surface; the boundary spec must stay green).
- [ ] Port with the established adaptation classes ONLY (ioc→constructor-injection; import paths; logging shim; verbatimModuleSyntax; the types trimmed at 2.2 get their stage interfaces COMPLETED now — reconcile `importStatement/types.ts` with the prior-art whole file, report the delta). Hunk-audit per file.
- [ ] Specs: Container wiring → direct construction (the 2.2 mechanical table approach); EVERY assertion preserved (1,347-line suite INTACT — zero weakening; report any genuinely-unportable test + why).
- [ ] **Recall mounts onto the stage:** at stage2 creation/mapping-start, `recallFor(columnNames)` prefills column definitions+params with GUESSED state (read how prior-art stage2 receives initial definitions; wire minimally — REPORT the mount point). New tests: stage2 starts with recalled prefills; N-of-M exposed; confirming a GUESSED column transitions state; applied+confirmed mappings flow to `savePool` (the learning loop — REPORT where save hooks: on apply? on stage completion? mirror prior-art's recall-save timing, adapted to per-column).
- [ ] Gate + commit: `feat(2.3): stage orchestration ported INTACT (1,347-line suite) + recall mounted (rxjs internal)`.

## Task 4: Row-generator — PORT + REBUILD (collect-don't-throw + COUNTERPARTY)

**Files:** PORT+REBUILD `internal/importStatement/stage3/row-generator.ts` (+spec REBUILT) + `stage3/types.ts` (the transaction-row shape — port the minimal closure; categorization types stay behind). PORT `service.stage3.spec.ts` if it tests row generation (else defer with a note).

- [ ] Read prior-art `row-generator.ts` (291) + spec (423) fully FIRST. The REBUILD contract (FEAT-022): a bad row produces a per-row error entry `{ rowIndex, errors: [...] }` and generation CONTINUES; output = `{ rows: TransactionRow[], rowErrors: RowError[] }`. Every prior-art assertion either preserved or explicitly superseded by the collect contract — enumerate the superseded ones in the report.
- [ ] **COUNTERPARTY output field:** generated rows carry `counterparty` distinct from `description` (the 2.2 carry-in completes). TIME: assert no time field in generated rows (extend the 2.2 verifiable-discard one level deeper).
- [ ] Income rows: the 2.2 label-and-discard reasons surface as row-level skip entries (not errors — a distinct `skipped` collection with reasons; pin VIS-011 semantics end-to-end).
- [ ] Gate + commit: `feat(2.3): row-generator rebuilt — collect-don't-throw (FEAT-022) + COUNTERPARTY field`.

## Task 5: E2E closure — fixtures + the extended real-statement local spec

**Files:** NEW `internal/importStatement/pipeline-e2e.spec.ts` + EXTEND `internal/ingest/real-statements.local.spec.ts`.

- [ ] Fixture E2E: `mono-like-utf8.csv` (and one spreadsheet fixture) through decode→service→stage1→stage2 (recall-prefilled on a seeded pool + manual completion)→row-generator → typed rows; hard assertions: row counts, a spot row's fields (date ISO, amount, counterparty present, NO time), N-of-M label, the placeholder-row skip carried through.
- [ ] Map-once-reimport-prefilled E2E: run mapping once (savePool), re-run the same fixture → prefills GUESSED with n=M.
- [ ] **Real-statement local spec (founder acceptance addition):** extend with the end-to-end flow per file — decode→typed rows with HARD typed-row counts baked from a local run (the 2.1 pattern one level deeper: mono UA expected typed-row count after income-skips/placeholder, mono EN, ukrsib — RUN locally, observe, bake, assert). Compact per-file summary printed for the QA doc.
- [ ] Gate + commit: `test(2.3): pipeline E2E — fixtures + real statements (hard typed-row counts)`.

## Task 6: Final gate + DoD (→ dev review → QA)

- [ ] Clean install (full rm list) → full gate all 0. Counts reported per package (engine grows ~100+; web 66; functions 73 — both untouched).
- [ ] DoD assertions: boundary/barrel/engine-client diffs vs main → empty · `grep -rn "from 'rxjs'" packages/engine/src/index.ts packages/engine/src/client packages/engine/src/qa.ts` → empty (no RxJS at the surface) · migration: v1/v2 steps byte-unchanged vs main (diff shows only the v3 append + related) · `grep -rn "Math.random\|Date.now" packages/engine/src --include="*.ts" | grep -v spec` → classify (determinism) · `git status --porcelain` clean.
- [ ] Report for dev final review (opus) → fixes → hand to ABC QA (protocol heart: E2E real-statement run, collision/NFC matrix, fresh≡upgrade at v3).

## Carry-forward

Thresholds into the store → 2.4 · pseudo-ops extend the row-generator → 2.5 · client surface + worker + **luxon build check (re-tagged)** + `./qa` sunset → 2.6 · S3a gate UI / S3b confirm copy → 2.7/2.8 · col_N cross-bank position-dependence: accepted, revisit on evidence.
