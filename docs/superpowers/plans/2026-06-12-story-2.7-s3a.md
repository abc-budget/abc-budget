# Story 2.7 — S3a Source UI — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** S3a as wizard step 1 over the worker client — file/sample → decode (with live progress) → recall summary → real gate #1; cold-start base-currency gate; contract v3 (base-currency surface + structural-error channel); the full state matrix reachable for QA clicks + the PM pixel pass (uk/en, desktop + ~390px).

**Architecture:** Engine first (Task 1: contract v3 — the UI builds on it), then the UI in layers: presentational components vs the bundle (Task 2), the state machine + wizard integration + gates (Task 3), the cold-start dialog (Task 4), E2E + pixel-pass script + final gate (Task 5). Visual truth = `D:\abc-budget\design-reference\{Import-Source.html, s3a-app.jsx, s3a-i18n.jsx}`; KB wins on conflicts; altus components from 1.3 reused, new CSS only as a declared delta (the 1.3 altus.css delta-header discipline).

**Tech Stack:** existing only — React 19 + react-router v7 (useBlocker), altus components, the 1.4 i18n catalog, worker client (2.6), @vitest/web-worker for engine tests, Testing Library for web.

**Source spec:** `docs/superpowers/specs/2026-06-12-story-2.7-s3a-design.md` (3 locked decisions + 5 endorsed conventions + the PROGRESS-DURING-DECODE pin)
**Branch:** `story/2.7-s3a`. **Flow:** tasks → dev final review (pixel-pass script + state matrix) → STOP, hand back to founder (role fence — dev does NOT run QA).

---

## Task 1: Contract v3 — base-currency surface + structural channel (engine, TDD)

**Files:** MODIFY `client/protocol.ts` (CONTRACT_VERSION → 3; methods += getBaseCurrency, setBaseCurrency) · `client/engine-client.ts` · `client/dto.ts` (GenerateResultDTO += structuralErrors) · `client/direct-client.ts` + `internal/worker/engine-worker-host.ts` (implement both methods via the composition root's settingsDao; serialize structuralErrors) · `internal/importStatement/stage3/{row-generator.ts, types.ts}` (structural channel) · `internal/settings/base-currency.ts` (a `getBaseCurrencyOrNull(dao)` variant — the client probe; the loud-throw variant stays for use_base) · `index.ts` barrel += `localeToCurrency` re-export · BOTH boundary specs (exact runtime keys incl. localeToCurrency) · the 2.4-era E2E assertion (DECLARED migration).

- [ ] **Structural channel (decision 2, failing tests first):** `GenerateRowsResult`/`GenerateResultDTO` += `structuralErrors: Message[]` / serialized. `generateRows`: BEFORE the row loop, count DATE-definition columns — 0 → push `engine.importStatement.stage3.structural-no-date-column` (ДІЯ: map a column as DATE) and return early with empty rows + the one message; >1 → `...structural-multiple-date-columns` (ДІЯ: unmap one) ditto. PINS: (a) the two conditions are DISTINCT messages (both asserted); (b) zero row-error echoes (rowErrors empty in both cases); (c) the DECLARED migration — find the pipeline-e2e assertion `rowErrors.length === 12` for the ignored-DATE case → becomes `structuralErrors.length === 1 && rowErrors.length === 0`, with a `// DECLARED CHANGE (2.7 decision 2)` callout naming the old behavior.
- [ ] **Base-currency surface (decision 1, failing tests first):** protocol v3 (bump rule header notes the v2→v3 history); `getBaseCurrency(): Promise<string|null>` (null when unset — implement via `getBaseCurrencyOrNull`); `setBaseCurrency(iso): Promise<void>` (validates via the 1.6 reference; invalid → the existing `InvalidBaseCurrencyError` crossing the wire typed — add it to the error codec). Both implemented in direct client AND worker host. Handshake tests update to contract 3 (the web router.spec CONTRACT assertion too — declared).
- [ ] **Live-read pin (decision 1):** real-hop test — `setBaseCurrency('PLN')` → IMMEDIATELY `importStart` + map a use_base AMOUNT column → resolved currency is PLN (no stale snapshot).
- [ ] Barrel += `localeToCurrency` (runtime); BOTH boundary specs → runtime keys exactly `['createDirectEngineClient','createWorkerEngineClient','localeToCurrency']` (sorted), failing first.
- [ ] Gate + commit: `feat(2.7): contract v3 — base-currency surface + structural-error channel (declared E2E migration)`.

## Task 2: S3a presentational components (vs the bundle)

**Files:** NEW `apps/web/src/app/screens/import/s3a/{DropZone,FileChip,RecognizedPanel,UnknownPanel,ErrorPanel,DecodingPanel}.tsx` (+ co-located specs) · NEW altus delta CSS in the established delta-header pattern (s3a classes from Import-Source.html: dropzone, filechip, savedmap, panel recog/unknown/errpanel, dedup, proceed-note, statebar excluded — prototype affordance) · MODIFY the i18n catalogs (uk/en — port s3a-i18n.jsx keys incl. function-style `recogTitle(n,m)` as parameterized entries per the 1.4 pattern).

- [ ] Port each component from `s3a-app.jsx` PRESENTATIONALLY (props in, callbacks out, zero engine imports — the 1.3 app-agnostic altus discipline applies to the new CSS, screen components live app-side): DropZone (drag states, pick button, formats note, local-only lamp, sample link) · FileChip (name/sizeLabel/rows + replace/remove) · RecognizedPanel (N-of-M header, recogTitle, partial lamp when n<m, savedmap rows name→type with «recalled» tag / UNKNOWN style, dedup info block, proceed note) · UnknownPanel (0-of-M, gold lamp, savedmap all-unknown, next-step CRT note) · ErrorPanel (ЩО/ЧОМУ/ДІЯ rows from props + retry) · DecodingPanel (NEW vs the bundle — the file-read progress visual: progress count over total, honest numbers from props; visually consistent with the bundle's plate/lamp language — flag in the report as the one designed-by-dev surface, PM eyeballs it at the pixel pass).
- [ ] Component specs: render per state incl. uk/en key presence, partial badge at n<m, savedmap row variants, error rows, a11y roles (the bundle's aria patterns).
- [ ] TYPE_LABEL mapping: column-definition → localized type label (from s3a-i18n's TYPE_LABEL — port to the catalog; covers all 2.2 ColumnDefinitions, not just the prototype's subset — enumerate against the enum, missing ones fail the build via exhaustiveness).
- [ ] Gate + commit: `feat(2.7): S3a presentational components vs the bundle (uk/en catalog, altus delta)`.

## Task 3: State machine + wizard integration + gates (TDD)

**Files:** NEW `s3a/S3aSource.tsx` (the container) · NEW `s3a/use-s3a-session.ts` (the hook owning the session lifecycle) · MODIFY `screens/ImportFlow.tsx` (per-step bodies; real canAdvance #1; useBlocker) · MODIFY router/spec as needed.

- [ ] `use-s3a-session.ts` (failing tests first, engine client mocked at the EngineClient interface): states `idle | decoding | recognized | unknown | error`; `onFile(file)` → read bytes → `decode` (progress events with the call's jobId update `{done,total}` state — PROGRESS-DURING-DECODE) → loud issues → error state (ЩО/ЧОМУ/ДІЯ from the decode issue keys); else `importStart` → snapshot → recognized (n>0) | unknown (n===0); `onSample()` → fetch the bundled asset → same path; `replace/remove` → `importAbort` (if session) → idle; exposes `{state, file, progress, snapshot, sessionId, error}`.
- [ ] Sample asset: NEW `apps/web/public/sample-statement.csv` — synthetic uk-flavored mono-like (header names matching the engine's recognizable shapes; ~30 rows; NO real data; deterministic content checked in); verify it lands in the Workbox precache (extend the build-check script's asset list).
- [ ] `S3aSource.tsx`: maps hook state → Task-2 components; otherSheets neutral note; partial vs full recognized (same panel, partial lamp per bundle).
- [ ] `ImportFlow.tsx`: step bodies become components (s3a real; s3b/c/d keep placeholder Panels); `canAdvance` per step — step 0: `state === 'recognized' || state === 'unknown'`; Next disabled otherwise (visual per 1.5 Key disabled pattern); «Назад» on step 0 → /dashboard (existing); session + snapshot pass down via context or props for 2.8 (report the shape — minimal, S3b consumes it next story).
- [ ] `useBlocker`: active session + navigating away from /import → confirm dialog (altus modal, uk/en); confirm → `importAbort` + proceed; cancel → stay. Tests: blocked/confirmed/cancelled paths.
- [ ] canAdvance matrix tests + state-transition tests (drop→decoding→recognized; bad file→error→retry→idle; remove→abort called).
- [ ] Gate + commit: `feat(2.7): S3a wired — session hook, real gate #1, useBlocker exit-protection (1.5 stub dies)`.

## Task 4: Cold-start base-currency gate (TDD)

**Files:** NEW `s3a/BaseCurrencyDialog.tsx` (+spec) · MODIFY `S3aSource.tsx`/`ImportFlow.tsx` (the probe-on-entry) · i18n keys (baseTitle/baseBody/baseLabel/baseAuto/cancel/cont from s3a-i18n).

- [ ] Dialog per the bundle's BaseCurrencyDialog: modal scrim + screws + lamp; native select with TWO optgroups (decision 3): curated 8 in prototype order (labels from the 1.6 reference, localized uk/en — NEVER hardcoded), divider, the rest of the reference sorted; preselect = `localeToCurrency(navigator.language)` ?? 'USD' (report the fallback choice); Confirm → `client.setBaseCurrency(iso)` → close; Cancel → `navigate('/')` (Onboarding pre-data, verified).
- [ ] Probe on /import entry: `getBaseCurrency()` → null → dialog BEFORE any file work (DropZone inert behind the scrim); non-null → no dialog. Tests: unset→dialog, set→none, confirm persists (mock client asserts setBaseCurrency called + dialog closes + S3a active), cancel navigates.
- [ ] **LOCKOUT PIN (decision 3):** locale `'ka-GE'`-style mapping outside the curated 8 → dialog preselects it in the lower optgroup → confirm → flow completes end-to-end (one test through the mocked client; the real-currency mapping asserted via the actual localeToCurrency).
- [ ] Gate + commit: `feat(2.7): cold-start base-currency gate — curated-8 + full reference, locale preselect (ENT-019)`.

## Task 5: E2E + pixel-pass script + final gate (→ dev final review → STOP)

- [ ] **PROGRESS PIN (founder, verbatim):** preferred route — the 2.6 Playwright pattern (real browser, real worker, real render): `vite preview` → drive S3a with the 10k synthetic → assert ≥1 intermediate progress render (DecodingPanel with done<total visible) BEFORE the summary appears; capture evidence. Fallback if the Playwright harness can't drive a file drop: engine-side real-hop test asserts ≥1 intermediate progress EVENT before decode resolves AND the web hook test asserts an intermediate progress RENDER from a synthetic event sequence — the pin then holds as the composition of the two (report which route landed).
- [ ] Web E2E (Testing Library, mocked client at the interface): the full state matrix — no file / decoding / recognized (n=m) / partial (0<n<m) / unknown (n=0) / read error / sample / first-run gate — each reachable and asserted in uk AND en.
- [ ] **PM pixel-pass script** (deliverable for the final review, manual steps doc — NOT committed app code): exact steps + files to reach every state at desktop and ~390px (devtools), incl. first-run (clear IDB userSettings), recognized (import the sample twice — pool warm), partial (a file sharing SOME column names with the sample), unknown (novel column names), error (a .pdf), decode-progress (the 10k synthetic — provide the generator command), uk/en toggle points. Save as `docs/superpowers/2026-06-12-s3a-pixel-pass-script.md`.
- [ ] Clean install → full gate: typecheck 0 · lint 0 · engine (931+new) · web (75+new) · functions 73 · build + build-checks (incl. the sample asset precached) · cold ×2.
- [ ] DoD sweep: boundary exactness (3 runtime keys) · contract 3 everywhere (no stray 2s — grep) · determinism 2 sites · migrations untouched · fence intact · `git status` clean.
- [ ] Dev final review report (pins scoreboard + state-matrix evidence + the pixel-pass script) → **STOP. Hand back to founder. Dev does NOT dispatch QA (role fence).**

## Carry-forward

S4 guarded base-currency change + searchable picker · real dedup counts (FEAT-018) → EP-3 · S3b consumes the session/snapshot context → 2.8 · multi-sheet selection → Designer residual on evidence · worker remote-rates wiring → EP-3 (standing) · Q-011 dup-counter → EP-3 (standing).
