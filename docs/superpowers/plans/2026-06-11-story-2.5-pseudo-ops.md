# Story 2.5 — Pseudo-ops expansion — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** BANK_COMMISSION/CASHBACK cells spawn separate pseudo-operations (ENT-013) under the 2.3 never-throw contract; `+`-prefixed amounts parse (declared divergence); Q-011 type-marker enters the hash; checkpoint-(b) full-pipeline evidence closes the engine epic phase.

**Architecture:** Order: parseNumber `+` first (lowest layer, everything numeric flows through it), then the hash discriminator (recipe change touches all hashes — land before pseudo-ops pin theirs), then the expander + row-generator wiring, then E2E + checkpoint-(b) evidence. Pure engine-internal: no UI, no migration, no public-surface change, no new deps. Ported suites stay green unweakened except declared pins.

**Tech Stack:** existing only — vitest + fake-indexeddb, $t/LocalizableException machinery, stage3 hash (WebCrypto SHA-256).

**Source spec:** `docs/superpowers/specs/2026-06-11-story-2.5-pseudo-ops-design.md` (3 locked decisions + pins + RowError columnId amendment)
**Branch:** `story/2.5-pseudo-ops`. **Flow:** tasks → dev final review WITH checkpoint-(b) evidence → STOP, hand back to founder (role fence — dev does NOT run QA).

---

## Task 1: parseNumber accepts leading `+` (decision 1, TDD)

**Files:** MODIFY `internal/utils/numbers/parsing.ts` (declared divergence — update the "ported verbatim / zero changes" header honestly) + `parsing.spec.ts` · one composition test in `internal/importStatement/stage2/column.spec.ts` (or errors.spec.ts — wherever mixed-type tests live; report).

- [ ] **Failing tests first** (parsing.spec.ts): `parseNumber('+5000,00')` → 5000 · `'+1 234.56'` → 1234.56 · `'+'` alone → NaN · `'++5'` → NaN · `'5+'` → NaN (sign must be leading, single) · existing `'-'` cases unchanged · `'+NaN'` still NaN-equivalent via NAN_STRING_VALUES (already listed — assert no regression).
- [ ] Implement — ONE RULE, ALL NUMERIC PATHS, at the parseNumber level: extend the character-allowlist regex to admit `+` and strip a single leading `+` before the existing pipeline (`-` continues to work as today; `+` elsewhere in the string → NaN). Update the file header: was "verbatim, zero changes" → now "one declared divergence (2.5, decision 1): leading `+` accepted — prior art rejected it; rationale: composed with the 2.4 error-%-gate, loud-NaN = column lockout for `+`-writing banks".
- [ ] **Composition pin (decision 1.2, one test):** a MIXED-type AMOUNT column where the income cell is `'+5000,00'` → that cell is labeled-and-discarded as INCOME with reason (VIS-011), NOT an error cell. Sign semantics unchanged: `+` = positive notation.
- [ ] Gate + commit: `feat(2.5): parseNumber accepts leading '+' — declared divergence (gate-lockout rationale); sign semantics unchanged`.

## Task 2: Q-011 type-marker in the hash (decision 2, TDD)

**Files:** MODIFY `internal/importStatement/stage3/hash.ts` + `hash.spec.ts`.

- [ ] **Failing tests first** (hash.spec.ts): canonical object carries a discriminator key `pseudoOp: 'main'` for main ops · `calculateRowHash(row, columns)` (existing signature, discriminator defaults `'main'`) ≠ hash of the same row with discriminator `'commission'` ≠ `'cashback'` (THE 3-distinct-hashes pin, decision 2) · two different rows both hashed as `'commission'` → distinct (same-type cross-row pin — they differ by their own fields) · existing hash tests updated ONLY where the recipe change shifts literal expectations (none are baked literals today — run-to-run comparisons stay valid; report if otherwise).
- [ ] Implement: `generateHashableObject(row, columns, discriminator: PseudoOpKind = 'main')` adds `pseudoOp: discriminator` to the canonical object; `calculateRowHash` gains the same optional param; `export type PseudoOpKind = 'main' | 'commission' | 'cashback'`. Existing callers (row-generator) untouched — default `'main'`.
- [ ] EP-3 ledger line in this plan's Carry-forward is already present — verify the commit message references it.
- [ ] Gate + commit: `feat(2.5): Q-011 type-marker — pseudo-op discriminator in the canonical hash (dup-counter → EP-3)`.

## Task 3: The expander + row-generator wiring (decisions 2+3 + ENT-013 core, TDD)

**Files:** NEW `internal/importStatement/stage3/pseudo-ops.ts` (+ NEW `pseudo-ops.spec.ts`) · MODIFY `stage3/row-generator.ts` (wiring + spawn points) · MODIFY `stage3/types.ts` (RowError += optional `columnId?: string`).

- [ ] `types.ts`: `RowError` gains `readonly columnId?: string` (spec amendment — cell coordinate; optional → zero churn in 2.3/2.4 suites). One pin: a failed pseudo-op's rowError carries the commission/cashback column id; main-op rowErrors leave it undefined.
- [ ] **Failing suite first** (`pseudo-ops.spec.ts`) — `expandPseudoOps(row, columns, baseCurrency): Promise<{ ops: TransactionRow[]; errors: RowError[] }>` pure helper:
  - row with non-empty commission cell → one op: `isBankCommission: true`, `description === $t('engine.importStatement.pseudo-op.bank-commission').getText()` (key string, hash-stable), date/account/source from original, `amount` = abs(cell), currency per the COLUMN's ENT-011 params (`auto` → plain number falls back per the existing AMOUNT machinery; `use_base` → resolved base; `{code}` → validated code — mirror/adapt `detectAmountAndCurrency`, report the reuse shape), counterparty/bankCategory/mcc null, hash via discriminator `'commission'`.
  - cashback ditto with `isCashback: true`, `.cashback` key («Кешбек»), discriminator `'cashback'`.
  - both cells non-empty → 2 ops, ENT-013 ordering (commission then cashback).
  - empty (null) cells → zero ops, zero errors.
  - negative commission cell (−100) → abs 100 (abs-amounts convention).
  - failed pseudo-op (cell with stage2 error Message / currency resolution throw) → `errors` entry `{ rowIndex, columnId: <that column>, errors: [...] }`; the OTHER pseudo-op still generated (per-operation granularity).
  - determinism: same row twice → deep-equal incl. hashes.
- [ ] **Wire into `generateRows`** (failing tests in row-generator.spec.ts first):
  - after a main op generates → expand; append ops in order main, commission, cashback (then the existing re-index pass syncs rowIndex).
  - **SPAWN-SCOPE PIN 1 (decision 3, verbatim):** income-skipped main + commission cell → main in `skipped` WITH reason AND exactly one commission pseudo-op in `rows`; same for cashback; both cells → 2 pseudo-ops + skipped main.
  - **SPAWN-SCOPE PIN 2:** errored row + non-empty commission cell → rowError only, ZERO pseudo-ops.
  - ENT-013 acceptance: fixture-shaped row with commission+cashback → exactly 3 ops, fields/flags exact; 3 DISTINCT hashes (the Q-011 pin at generateRows level).
  - never-throw holds: a bad commission cell on a good main → main in `rows`, pseudo-error in `rowErrors` (with columnId), generation continues.
- [ ] Gate + commit: `feat(2.5): pseudo-ops expansion — commission/cashback spawn independent ops (ENT-013; spawn incl. skipped mains)`.

## Task 4: E2E + checkpoint-(b) evidence

**Files:** EXTEND `internal/importStatement/pipeline-e2e.spec.ts` · NEW fixture `income-commission.csv` (+ matrix README row — decision 3 pin 3) · EXTEND `internal/ingest/real-statements.local.spec.ts`.

- [ ] NEW fixture `income-commission.csv`: deterministic synthetic — rows incl. (a) income main (+ positive amount) WITH commission cell, (b) outcome main with cashback, (c) outcome with both, (d) plain outcome, (e) a `+`-prefixed cashback value «+50,00» (decision 1 in fixture form). Document in `fixtures/README.md` (the QA matrix source).
- [ ] Fixture E2E (failing first): `mono-like-utf8.csv` (has both columns) full pipeline decode→stage1→stage2(map commission+cashback columns)→generateRows → hard counts: main ops + commission ops + cashback ops + skipped + rowErrors, each exact · `income-commission.csv` → the spawn-scope matrix end-to-end (income-skipped main spawns its commission; `+50,00` cashback parses and spawns).
- [ ] **Real-statements extension (checkpoint-(b) evidence):** per real file, extend the E2E flow to map BANK_COMMISSION/CASHBACK columns where present (mono UA/EN: «Сума комісій»/«Сума кешбеку» — map them; ukrsib: inspect locally, map what exists) → RUN locally, OBSERVE, BAKE hard counts: typed mains / commission ops / cashback ops / skipped / rowErrors per file (mono UA expectation from raw cells: 27 cashback candidates — observed count wins). Print the per-file checkpoint-(b) summary block (decode → recall-relevant → typed mains → pseudo-ops → skipped/errors) for the dev-review report.
- [ ] Gate + commit: `test(2.5): E2E pseudo-ops — fixtures + real statements (checkpoint-b hard counts at every layer)`.

## Task 5: Final gate + DoD (→ dev final review with checkpoint-(b) → STOP)

- [ ] Clean install (full rm list incl. `*.tsbuildinfo`) → typecheck 0 · lint 0 · engine (784+new) · web 66 · functions 73 · build green.
- [ ] DoD: boundary/barrel/client/qa diff vs main → 0 · engine-db.ts diff vs main → 0 (no migration touch at all this story) · determinism grep → the three known classified sites only · ported suites unweakened (declared pins only: the parseNumber divergence tests; report any other test deltas with justification) · `git status --porcelain` clean.
- [ ] Dev final review report WITH the checkpoint-(b) evidence block (fixtures + three real statements: decode → recall → thresholds → typed rows → pseudo-ops, hard counts at every layer) → **STOP. Hand back to founder. Dev does NOT dispatch QA (role fence).**

## Carry-forward

**Q-011 second half → EP-3 (founder ledger tag, verbatim): dup-counter (identical full rows → suffix 0,1,2…) — "pin 'stable count, not order' against the real upsert path".** · categorization/filter rules over isBankCommission/isCashback → EP-4 · pseudo-op UI rendering → 2.7/2.8 · worker/client surface + production wiring → 2.6.
