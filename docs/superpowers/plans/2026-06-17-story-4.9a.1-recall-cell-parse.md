# Story 4.9a.1 — Recalled cells parse — FIX PLAN

> Root cause ACCEPTED; 3 rulings. Test-first (RED→GREEN guard FIRST). Branch
> `story/4.9a.1-recall-cell-parse` off `main` @ `797cfc2`. Root-cause:
> [`…-recall-cell-parse-rootcause.md`](../specs/2026-06-17-story-4.9a.1-recall-cell-parse-rootcause.md).

## Confirmed rulings
1. **Fix layer = `service.startWith`/`stage2()` recall step** running the EXTRACTED shared dispatcher
   (`definition → parse`), EXTRACTED from `direct-client.ts:164-189` (one helper, BOTH paths use it — no
   divergence). Run it for each recalled typed column BEFORE startWith returns (no consumer sees unparsed
   cells). The recall mount is the sole entry (`service.ts:189-201`); **verify the clone path
   `implementation.ts:530` carries already-parsed cells** (the invariant is "a typed-definition column never
   has unparsed cells", not "startWith parses").
2. **Separate parse from confirmation (PREFERRED):** the shared dispatcher PARSES ONLY (transforms cells per
   definition, touches NO `recallState`); the interactive path keeps its OWN `guessed→confirmed` transition
   (in `applyColumn`); the recall parse leaves `recallState:'guessed'` untouched, AND does NOT stage a pool
   write (the endorsement is the user's advance via `flushRecallWrites`). The entanglement: `parseGeneric`
   transforms THEN calls `applyColumn` (which confirms + stages). → factor the CELL-TRANSFORM out of
   `parseGeneric` so it can install parsed cells WITHOUT the confirm/pool-write. **Fallback (only if too
   entangled to separate cleanly): an explicit `keepRecallState` flag** on the apply that skips the
   transition + the pool-write. **REJECT parse-then-restamp-guessed** (a transient `confirmed` is a smell).
3. **Holistic:** parse DATE + AMOUNT + EVERY typed recalled column (not date-only).

## The fix
1. **Extract** `parseColumnByDefinition(col, definition, params)` (the `direct-client.ts:164-189` switch →
   `col.parseAsDate`/`parseAsAmount`/`parseAsCurrency`/`parseAsDescription`/`parseAsCounterparty`/
   `parseAsMerchant`/`parseAsBankCategory`/`parseAsBalance`/…) into ONE shared engine helper. `importApplyColumn`
   (interactive) calls it unchanged; the recall step calls it too.
2. **Parse recalled columns at the recall mount** (`service.ts` startWith/stage2, async, before return): for
   each prefilled column whose definition is typed, run the parse via the shared helper, PRESERVING
   `recallState:'guessed'` and staging NO pool write (Ruling 2 separation, or the `keepRecallState` flag).
3. Keep `extractDate`/`autoCategorize` UNCHANGED (no defensive coercion — the contract is restored at source).

## Tests (RED→GREEN — write the guard FIRST, prove it RED on main)
- **Regression guard (the headline):** seed the recall pool with a bank's mapping (DATE + AMOUNT + …); start
  a NEW import of the SAME statement (columns recall as `guessed`) → drive through `generateRows` → assert
  EACH `ImportStatementStage3Row.date instanceof Date` AND `typeof row.amount === 'number'`; then
  `footprintYearMonth(row.date)` / `autoCategorize` runs with NO throw. **MUST be RED on main today** (recalled
  date = string → `instanceof Date` false / `getUTC*` throws) → **GREEN after the fix**. Extend the existing
  recall e2e through stage3 typing + a focused stage2-recall-parse unit test.
- Recalled columns STAY `guessed` after the parse (not auto-confirmed); NO pool write is staged at recall
  mount (the learning loop still fires at advance/`flushRecallWrites`, unchanged).
- The interactive path is unchanged (its `guessed→confirmed` + pool-write still happen via `applyColumn`).
- The clone path (`implementation.ts:530`) carries parsed cells (no re-introduction of unparsed cells).

## DoD
- [ ] shared dispatcher extracted (one source); recalled DATE + AMOUNT (+ all typed) cells PARSED at the
      recall mount, BEFORE startWith returns; recalled columns stay `guessed`, no pool write at mount.
- [ ] the RED→GREEN regression guard (state the red-on-main evidence in Dev-complete).
- [ ] NO coercion in `extractDate`/`autoCategorize`; `CONTRACT_VERSION` unchanged; cold gate green (engine grows).
- [ ] REPORT: this also unblocks EP-5 (a recalled import's `amount` must be a number before amountUSD math) — flag, don't build.

## Role fence
Dev implements + self-tests only. No QA 4-pass, no QA report, no merge. Hand back when In-QA-ready.
