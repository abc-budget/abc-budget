# Story 2.2 — Column-type catalog — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** The ported+extended ColumnDefinition catalog ≡ ENT-009 (16 types), the crown-jewel column transforms (locale numbers, auto date detect, amount semantics, per-column errors), TIME verifiably discarded, COUNTERPARTY as a distinct field, ENT-011 wired to the 1.6 resolver — engine-internal, deterministic (HC-9).

**Architecture:** Port prior-art `importStatement/types.ts` + `stage2/{column,row,types}.ts` + their specs and the ~400 utils lines they need, mirroring source paths under `internal/`. Five adaptations (logging shim, 1.6 currency hook, static ENT-016 config, lazy+pinned luxon, deterministic sampling replacement) — everything else verbatim, hunk-audited. New TIME/COUNTERPARTY transforms follow the existing per-type pattern.

**Tech Stack:** luxon ^3 (MIT, lazy `import()`, zone/locale pinned), ported utils (zero new deps beyond luxon), vitest.

**Source spec:** `docs/superpowers/specs/2026-06-11-story-2.2-column-catalog-design.md`
**Branch:** `story/2.2-column-catalog`. **Prior art (READ-ONLY):** `D:\abc-budget\webapp\libs\engine\src\importStatement\` + `D:\abc-budget\webapp\libs\utils\src\lib\`.
**Flow:** tasks → dev final review → ABC QA (ENT-009 diff; TIME-discard + determinism provokes) → PM.

---

## Task 1: Foundations — logging shim, engine-config, ported utils (TDD where new)

**Files:** `internal/logging.ts` (NEW), `internal/settings/engine-config.ts` (NEW), `internal/utils/{date/format-detector.ts, numbers/parsing.ts, collections/sampling.ts, messages/*}` (PORT; sampling body REPLACED).

- [ ] `logging.ts`: `getLogger(name): { debug/info/warn/error(...args): void }` — console-backed, no-op when `process.env.VITEST` (keeps ported logging lines intact and tests quiet). Match the prior-art call surface (read its usage in column.ts — only method calls, no config).
- [ ] `settings/engine-config.ts`: `getEngineConfig()` returning the ENT-016 defaults `{ acceptableParseDatePercentage: 90, acceptableColumnErrorPercentage: 0.3, successStatusThreshold: 0.8 }` (same shape as prior-art `settings/engine-config.ts` — read it and mirror the surface; constants only, JSDoc: store-backed at 2.4, NFR-009 never raw in UI).
- [ ] PORT utils with diff-audit: `numbers/parsing.ts` verbatim (+`isNan` and `NAN_STRING_VALUES` — check what column.ts imports and bring exactly the needed exports; bring `parsing.spec.ts` if it exists). `messages/` — bring `exceptions.ts`, `multiple-errors.ts`, the `Message`/`LocalizableMessage`/`$t` definitions (find `$t`'s file via grep in prior-art utils; port minimal closure of what column.ts + format-detector import; specs along).
- [ ] **`collections/sampling.ts` — REPLACE the body (founder-mandated):** keep the exact signature `sampleArray<T>(array, percentage, maxElements, minElements): T[]` and the same size-clamping math; replace the Math.random Fisher-Yates with **evenly-spaced deterministic selection**:
```ts
  // Deterministic evenly-spaced sampling (HC-9). Prior art used Math.random Fisher-Yates —
  // replaced 2026-06-11 (founder mandate): format detection must not vary run-to-run.
  const result: T[] = [];
  for (let i = 0; i < sampleSize; i++) {
    result.push(array[Math.floor((i * array.length) / sampleSize)]);
  }
  return result;
```
- [ ] **TDD the replacement:** `sampling.spec.ts` — pinned: same input → same output (twice); sampleSize math preserved (percentage/max/min clamps — port the prior-art spec's clamp cases if a spec exists, else write table-driven clamp tests); even spacing (e.g. 10-of-100 → indices 0,10,…,90); full-array when size ≥ length; empty input.
- [ ] PORT `date/format-detector.ts` with the luxon adaptation: the file is luxon-`DateTime`-based — make luxon **lazy**: the detector's public function becomes async (it likely is sync in prior art — if sync, wrap: `detectDateFormat` becomes async and column.ts's call site adapts; REPORT this signature delta) and every `DateTime.fromFormat` call gets `{ zone: 'utc', locale: 'en-US' }`. Add `pnpm --filter @abc-budget/engine add luxon` + `@types/luxon` devDep if types aren't bundled. Port its spec.
- [ ] Gate + commit: `feat(2.2): foundations — logging shim, ENT-016 config, ported utils (deterministic sampling, lazy pinned luxon)`.

## Task 2: Catalog types — PORT + extend to 16 (TDD structure test)

**Files:** `internal/importStatement/types.ts` (PORT+EXTEND), `internal/importStatement/stage2/types.ts` (PORT+EXTEND), `internal/importStatement/catalog.spec.ts` (NEW).

- [ ] **Failing structure test first** (`catalog.spec.ts`): asserts `ColumnDefinition` has EXACTLY the 16 spec-§5 values (enum string values pinned: `unknown,date,amount,description,currency,bank_account,merchant_category,category,balance,status,exchange_rate,bank_commission,cashback,time,counterparty,ignore`); param-shape assertions via representative typed literals for each params interface (compile-level: `DateColumnParams = { format: 'auto' } | { format: { custom: string } }`-shaped per canon — pin with `satisfies`-style fixtures + runtime key checks where shapes are runtime-visible).
- [ ] PORT `importStatement/types.ts`: enum + ALL params interfaces + the Stage2 interfaces column.ts/row.ts need (bring the minimal closure; the stage1/stage3 interfaces NOT needed by 2.2 stay behind — leave a `// 2.3 ports the rest` note if the file is trimmed; prefer porting the whole file verbatim if its extra interfaces compile standalone — report which). EXTEND: `TIME = 'time'`, `COUNTERPARTY = 'counterparty'`. ADAPT: `AmountColumnParams.currency` typed as the 1.6 `CurrencyDetectOptions` (import from `../currency/detect`); prior art's own CurrencyDetectOptions-ish type (if it exists in types.ts) is REPLACED by ours — diff-audit notes it.
- [ ] PORT `stage2/types.ts` (CellData, SupportedDataType) + EXTEND SupportedDataType if per-type output typing requires (TIME produces nothing; COUNTERPARTY → string — check how DESCRIPTION maps and mirror).
- [ ] Gate + commit: `feat(2.2): catalog types ≡ ENT-009 — ported + TIME/COUNTERPARTY (structure-tested)`.

## Task 3: The crown jewel — PORT column.ts + row.ts + their specs

**Files:** `internal/importStatement/stage2/column.ts` (PORT+ADAPT), `stage2/row.ts` (PORT), `stage2/column.spec.ts` + `stage2/row.spec.ts` (PORT+ADAPT).

- [ ] Copy `column.ts` (1,735 lines). Adaptations ONLY (hunk-audit EVERY change in the report):
  1. Import paths: `@abc-budget/logging` → `../../logging`; `@abc-budget/utils` → `../../utils/...` per Task 1 layout; `luxon` → keep type imports static (`import type`), runtime via the Task 1 lazy seam (if column.ts uses DateTime directly: route through a small `../../utils/date/luxon-lazy.ts` helper exporting `getDateTime(): Promise<typeof DateTime>` — created in Task 1 if needed; REPORT how many call sites).
  2. `CurrencyCache` → the 1.6 hook: read prior-art usage (how AMOUNT/CURRENCY transforms resolve currency); replace with `resolveCurrency`/`symbolToIso` from `../../currency/{detect,reference}` preserving behavior (the params carry `CurrencyDetectOptions`; `use_base` needs the base currency — check how prior art obtains it (engine config? param?) and mirror with our shape; REPORT the wiring).
  3. `getEngineConfig` → `../../settings/engine-config`.
  4. verbatimModuleSyntax `import type` fixes (report each).
  NOTHING else changes in ported code.
- [ ] EXTEND column.ts with the two new transforms following the existing per-type pattern exactly (read 2-3 existing `transformTo*` methods first; mirror validation/error structure):
  - `transformToTime()`: validates the column CAN be labeled (any cell content acceptable — it's a label); emits NO output cells (SupportedDataType: none / cells dropped). The transform result marks the column TIME with empty output.
  - `transformToCounterparty()`: DESCRIPTION-like text transform → output field/type `counterparty`.
- [ ] PORT `row.ts` verbatim (+import-path adaptation only).
- [ ] PORT both specs: Container/jest-era wiring → direct construction + `vi.*` (the 1.2/1.6 mechanical table approach); KEEP every assertion; specs needing luxon go async where the seam went async. Diff-audit.
- [ ] Gate + commit: `feat(2.2): port crown jewel — column transforms + row (1.6 currency hook, lazy luxon) + TIME/COUNTERPARTY`.

## Task 4: The new-behavior test suite (TDD against the ported impl)

**Files:** `internal/importStatement/stage2/catalog-behavior.spec.ts` (NEW).

- [ ] Write the spec-§6 new tests (these run against Task 3's implementation — failures = port/extension bugs; fix impl, never weaken):
  - **TIME verifiable discard:** type a 3-column fixture (date, time, amount) → typed rows contain NO time field; `JSON.stringify` of the full transform output does NOT contain any raw time value (`'14:55:43'` etc.).
  - **COUNTERPARTY distinct flow:** description + counterparty columns → both fields present and distinct in typed output.
  - **Amount semantics ×4:** income → rows skipped, each with a reason (FEAT-022 shape — assert the reason object exists per skipped row); outcome (all positive = debits); mixed (sign decides; positive/income side DISCARDED with reason, negative kept as expense); auto (deterministic detection: an all-negative-or-positive column → outcome; a both-signs column → mixed — pin with two fixtures).
  - **ENT-011 hook:** AMOUNT with `{ currency: 'auto' }` + a `грн` currency column resolves UAH; `use_base`; `{ code: 'PLN' }` override.
  - **Date-serial policy:** `"45307"` in a DATE column (format auto) → per-cell parse error, loud, never epoch-converted.
  - **col_N flow:** a column named `col_2` types like any named column.
  - **TZ-env determinism:** spawn a child vitest? NO — simpler: the test sets `process.env.TZ = 'America/New_York'` in an isolated spec file's beforeAll (node respects TZ at Date construction)… luxon with pinned `zone:'utc'` must produce identical parse output to the committed expectation (a fixed DD.MM.YYYY parse → pinned ISO string). Keep it one dedicated spec file (`tz-determinism.spec.ts`) so the env var can't bleed.
- [ ] Gate + commit: `test(2.2): catalog behavior — TIME discard, COUNTERPARTY, amount semantics, ENT-011, date-serial, col_N, TZ`.

## Task 5: Build discipline + final gate (⛳ → dev review → QA)

- [ ] Lazy-luxon build check: `pnpm build` → luxon is a SEPARATE chunk (not in boot), precached in sw.js (the 2.1 xlsx pattern — but NOTE: nothing in apps/web imports the catalog yet, so the luxon chunk may not appear in the web build AT ALL until 2.7 wires parsing; if absent, verify instead at the ENGINE level that no static runtime `from 'luxon'` import exists outside the lazy seam — source-discipline test like 2.1's — and note the web-build verification lands at 2.7). REPORT which case applies.
- [ ] Clean install + full gate (the standard rm list + functions) → all 0. Engine 312+new (report), web 64, functions 33.
- [ ] DoD assertions: boundary spec + barrel + engine-client diffs vs main → empty · `grep -rn "Math.random" packages/engine/src --include="*.ts" | grep -v spec` → empty (HC-9) · `grep -rn "from 'luxon'" packages/engine/src --include="*.ts"` → type-only/lazy-seam sites only (classify) · structure test green (catalog ≡ ENT-009).
- [ ] Report for dev final review → then hand to ABC QA (ENT-009 field-by-field diff; provoke targets: TIME discard, determinism).

## Carry-forward

`implementation.ts` + 1,347-line stage suite → 2.3 INTACT (tagged) · settings store (migration v3) → 2.3/2.4 · per-type help docs for time/counterparty → 2.8 · luxon web-build chunk verification → 2.7 (if Task 5 finds the chunk absent) · row-generator rebuild → with its stage.
