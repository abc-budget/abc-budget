# Story 2.4 — Column-error thresholds + rollback — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ENT-016 thresholds store-backed via a session-frozen hydrated snapshot; the >30% rejection carries complete per-cell evidence (typed exception); the Q-009 stop becomes structured (typed error + getter, one predicate).

**Architecture:** Pure engine-internal story — no UI, no worker, no public-surface change, NO new migration (overrides are rows in the v3 `userSettings` store). Order: config substrate first (everything reads it), then the rejection payload, then the Q-009 stop, then service hydration + E2E. The 1,347-line ported stage2 suite and the column suite must stay green UNWEAKENED throughout (`ColumnTransformRejection`/`UnmappedColumnsError` subclass `LocalizableException`, so existing `toThrow` pins hold).

**Tech Stack:** no new deps. Existing: vitest + fake-indexeddb, settings DAO (2.3), LocalizableException machinery (2.2).

**Source spec:** `docs/superpowers/specs/2026-06-11-story-2.4-thresholds-design.md` (4 locked decisions + 3 pins + founder's triple-pin note)
**Branch:** `story/2.4-thresholds`. **Flow:** tasks → dev final review → STOP, hand back to founder (role fence — dev does NOT run QA).

---

## Task 1: engine-config — store-backed hydrated snapshot (TDD)

**Files:** REBUILD `internal/settings/engine-config.ts` (+ NEW `engine-config.spec.ts`) · MODIFY `internal/settings/user-settings.ts` (SettingKeys only).

- [ ] `SettingKeys` += `ENGINE_ACCEPTABLE_PARSE_DATE_PERCENTAGE = 'engineConfig.acceptableParseDatePercentage'`, `ENGINE_ACCEPTABLE_COLUMN_ERROR_PERCENTAGE = 'engineConfig.acceptableColumnErrorPercentage'`, `ENGINE_SUCCESS_STATUS_THRESHOLD = 'engineConfig.successStatusThreshold'`, `ENGINE_RECALL_AUTO_DETECT_ENABLED = 'engineConfig.recallAutoDetectEnabled'`.
- [ ] **Failing suite first** (fake-indexeddb over the real v3 `userSettings` store via openEngineDb or a test DB — mirror the 2.3 settings spec pattern):
  - pre-hydrate: `getEngineConfig()` returns pure ENT-016 defaults (90 / 0.3 / 0.8 / false).
  - hydrate with empty store ≡ pure defaults (fresh-install determinism).
  - hydrate with stored overrides → snapshot = defaults ⊕ overrides (partial override: one key set, three default).
  - `setEngineParam` out-of-range → loud typed throw (`InvalidEngineParamError`), store UNTOUCHED, snapshot UNTOUCHED. Ranges: parseDate% ∈ [0,100]; error% and statusThreshold ∈ [0,1]; autoDetect boolean. Non-finite numbers rejected.
  - **THE TRIPLE PIN (founder note, verbatim):** (1) `setEngineParam` mid-session (= after hydrate) → same-session `getEngineConfig()` reads UNCHANGED; (2) the value IS in the store (read via DAO); (3) re-`hydrateEngineConfig` → new value visible. One test, three asserts, in that order.
  - `resetEngineConfigForTests()` restores pristine defaults (test seam, not on barrel).
- [ ] Implement: module-level `snapshot: EngineConfig` initialized to defaults; `hydrateEngineConfig(dao: UserSettingsDAO): Promise<void>` = the ONLY mutation point (reads 4 keys, overlays); `setEngineParam(dao, key, value): Promise<void>` validates LOUDLY then writes STORE ONLY; `getEngineConfig()` byte-compatible surface (returns the snapshot — all 6 call sites untouched, verify with grep). `EngineConfig` interface unchanged.
- [ ] Gate + commit: `feat(2.4): engine-config store-backed — hydrated snapshot, session-frozen, validating setEngineParam (ENT-016/NFR-009)`.

## Task 2: ColumnTransformRejection — the ENT-015 evidence payload (TDD)

**Files:** NEW `internal/importStatement/stage2/errors.ts` (+ NEW `errors.spec.ts`) · MODIFY `stage2/column.ts` (parseGeneric gate site ONLY — declared adaptation, hunk-audited).

- [ ] `errors.ts`: `class ColumnTransformRejection extends LocalizableException` with readonly `errorCount: number`, `totalCount: number`, `threshold: number`, `cellErrors: ReadonlyArray<{ rowIndex: number; error: Message }>`. Message = existing `too-many-parsing-errors` interpolation + a ДІЯ action-hint key (`engine.importStatement.transform-rejected-action` — new catalog entry, uk+en semantics like the 2.2 message work).
- [ ] **Failing tests first:**
  - \>30%-bad column apply → throws `ColumnTransformRejection`; exact `errorCount`/`totalCount`/`threshold`; `cellErrors.length === errorCount` (ALL collected, FEAT-022 complete-not-first); each entry's `rowIndex` correct and `error` is the per-cell Message.
  - <30% → applied, per-cell issues flagged on cells (re-assert existing behavior).
  - **Boundary pin (decision 2, verbatim):** two columns, one >30%-bad; apply to both → bad column throws with payload, its state stays UNKNOWN (definition null, data untouched); sibling column applies fine; stage2 alive (columns observable functional).
  - Store-backing proof: hydrate with `acceptableColumnErrorPercentage` override (e.g. 0.05) → a 10%-bad column now REJECTED; reset → same column applies. (Threshold demonstrably read from config, not constant.)
  - Existing suites: `toThrow(LocalizableException)` pins in the ported column/stage2 suites stay green (subclass) — run them, zero weakening.
- [ ] Implement: in `parseGeneric`, build `cellErrors` while iterating `newData` (index = rowIndex); replace the bare `validateErrorPercentage` throw with constructing `ColumnTransformRejection` (keep the helper; it returns the verdict or the gate site inlines — keep the diff minimal and hunk-audited; throw SEMANTICS unchanged).
- [ ] Gate + commit: `feat(2.4): >30% rejection carries complete per-cell evidence — ColumnTransformRejection (ENT-015, ЩО/ЧОМУ/ДІЯ)`.

## Task 3: Q-009 explicit stop — typed error + getter, ONE predicate (TDD)

**Files:** MODIFY `stage2/errors.ts` (+spec) · MODIFY `stage2/implementation.ts`.

- [ ] `errors.ts` += `class UnmappedColumnsError extends LocalizableException` with readonly `unmappedColumns: ReadonlyArray<{ id: string; name: string }>`; localizable message enumerating the names (new catalog key `engine.importStatement.unmapped-columns-stop`).
- [ ] **Failing tests first:**
  - stage2 with 2 unmapped columns: `getUnmappedColumns()` returns both `{id, name}`; `next()` throws `UnmappedColumnsError`; `error.unmappedColumns` deep-equals the getter's list.
  - **⟺ pin (decision 3, verbatim):** `getUnmappedColumns().length === 0 ⟺ next() does not throw` — both directions: all-mapped stage2 → getter empty AND next() resolves; one unmapped → getter non-empty AND next() throws.
  - The ported suite's existing next()-throws test stays green (subclass of Error; check its assertion — if it pins the old message string, that is THE one legit update this story, call out in the commit).
- [ ] Implement: ONE predicate — private `_unmappedColumns()` on the impl; `getUnmappedColumns()` returns it; `next()` throws `UnmappedColumnsError(list)` when non-empty; the existing static `areAllColumnsProcessed` delegates to the same logic (or is absorbed — keep whichever the ported suite pins, but ONE source of truth).
- [ ] Gate + commit: `feat(2.4): Q-009 explicit stop — UnmappedColumnsError + getUnmappedColumns off one predicate`.

## Task 4: Service hydration + E2E closure

**Files:** MODIFY `importStatement/service.ts` (UserSettingsDAO injection + hydrate at import-session start) · EXTEND `internal/importStatement/pipeline-e2e.spec.ts` · NEW fixture `bad-dates.csv` (synthetic, deterministic: a date column with >30% malformed values among well-formed rows) if no existing fixture qualifies.

- [ ] `service.ts`: accept `settingsDao: UserSettingsDAO | null` (constructor injection, default null — production wiring is 2.6); at import-session entry (the method that creates stage1 from decoded data — locate, REPORT the exact hook point), `await hydrateEngineConfig(settingsDao)` when dao present. Null dao → defaults, no throw (deterministic baseline).
- [ ] Engine-init hydrate (spec: "engine init AND import-session start"): in `persistence/engine-db.ts` `doInit()`, after openEngineDb succeeds, construct a `UserSettingsIDB` over the open DB and `hydrateEngineConfig` — failure NON-FATAL but LOUD (logged via the shim; defaults stand — HC-7 pattern, mirror the 2.3 savePool catch). One test: init with a store override → `getEngineConfig()` reflects it post-init.
- [ ] E2E (failing first):
  - bad-dates fixture through decode→stage1→stage2 → applying DATE to the bad column throws `ColumnTransformRejection` (payload spot-checked) → map the column as IGNORE instead → session completes to typed rows (per-COLUMN boundary at E2E level).
  - Store-override E2E: seed `engineConfig.acceptableColumnErrorPercentage` low via DAO BEFORE session start → a mildly-bad column that applies under defaults is now rejected (store-backing live end-to-end).
  - **Triple pin at E2E level:** mid-session `setEngineParam` → current session's subsequent applies use the old threshold; new session (re-import) → new threshold bites.
  - Q-009 E2E: leave one column UNKNOWN → `next()` throws `UnmappedColumnsError` naming it; map it → `next()` resolves.
- [ ] Gate + commit: `test(2.4): E2E — store-backed thresholds drive the gate; session-frozen config + Q-009 stop end-to-end`.

## Task 5: Final gate + DoD (→ dev final review → STOP)

- [ ] Clean install (full rm list incl. `*.tsbuildinfo`) → typecheck 0 · lint 0 · engine (720+new) · web 66 · functions 73 · build green.
- [ ] DoD assertions: `git diff main -- packages/engine/src/index.ts packages/engine/src/client packages/engine/src/qa.ts packages/engine/src/boundary.spec.ts` → empty · `git diff main -- packages/engine/src/internal/persistence/engine-db.ts` → EMPTY (no new migration — explicit DoD line) · determinism grep (`Math.random|Date.now` non-spec) → classify, expect the three known 2.3-classified sites only · real-statements local spec still green (thresholds at defaults — counts unchanged) · `git status --porcelain` clean.
- [ ] Dev final review report (per-decision evidence: the three pins + triple, suite-integrity statement) → **STOP. Hand back to founder. Dev does NOT dispatch QA (role fence).**

## Carry-forward

Production DAO wiring + store-validator registration at service construction → 2.6 · rollback surfacing UI (ЩО/ЧОМУ/ДІЯ render) + Option-A gate → 2.8 · presets/safe-abstraction settings UI → post-EP-2 · almost-over-budget threshold → EP-6 · KB WATCH: error-% denominator (body-dense empty rows) — revisit on evidence.
