{
  "name": "@abc-budget/root",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "pnpm --filter @abc-budget/web dev",
    "build": "tsc -b && pnpm --filter @abc-budget/web build",
    "preview": "pnpm --filter @abc-budget/web preview",
    "test": "pnpm -r test",
    "test:functions": "npm --prefix functions run test",
    "typecheck": "tsc -b",
    "lint": "eslint .",
    "make-icons": "node tools/make-icons.mjs",
    "gen:mcc": "node tools/gen-mcc-reference.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.18.0",
    "eslint": "^9.17.0"
  }
}
